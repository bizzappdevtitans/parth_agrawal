<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <!-- server action to confirm freight order -->
        <record id="action_confirm_freight_order" model="ir.actions.server">
            <field name="name">Confirm Freight Order</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_freight_order" />
            <field name="binding_model_id" ref="model_freight_order" />
            <field name="state">code</field>
            <field name="code">if not env.user.has_group('freight_management.group_freight_admin'):
    records.access_error()
if  env.user.has_group('freight_management.group_freight_admin'):
    records.action_confirm_order()</field>
        </record>
        <!-- server action to complete freight order -->
        <record id="action_done_freight_order" model="ir.actions.server">
            <field name="name">Complete Freight Order</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_freight_order" />
            <field name="binding_model_id" ref="model_freight_order" />
            <field name="state">code</field>
            <field name="code">if not env.user.has_group('freight_management.group_freight_admin'):
    records.access_error()
if  env.user.has_group('freight_management.group_freight_admin'):
    records.action_done_order()</field>
        </record>
        <!-- server action to cancel freight order -->
        <record id="action_cancel_freight_order" model="ir.actions.server">
            <field name="name">Cancel Freight Order</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_freight_order" />
            <field name="binding_model_id" ref="model_freight_order" />
            <field name="state">code</field>
            <field name="code">if not env.user.has_group('freight_management.group_freight_admin'):
    records.access_error()
if  env.user.has_group('freight_management.group_freight_admin'):
    records.action_cancel_order()</field>
        </record>

</odoo>
